package de.prog2.dungeontop.control.userinput;

import de.prog2.dungeontop.resources.LoggerStringValues;
import de.prog2.dungeontop.utils.GlobalLogger;
//import javafx.event.EventHandler;
//import javafx.scene.input.KeyEvent;

public class KeyHandler //implements EventHandler<KeyEvent>
{

    // TODO: KeyInput Handling e.g. storing currently pressed inputs or last input
    // DONE: Keystroke recognition
    /**
     * Method to Handle the inputs of a user via the keyboard.
     *
     * @param keyEvent Event which indicates that a keystroke occured
     */
//    @java.lang.Override
//    public void handle (KeyEvent keyEvent) {
//        if(KeyEvent.KEY_PRESSED.equals(keyEvent.getEventType())){
//            GlobalLogger.log(keyEvent.getCharacter() + LoggerStringValues.PRESSED);
//        } else if (KeyEvent.KEY_RELEASED.equals(keyEvent.getEventType())) {
//            GlobalLogger.log(keyEvent.getCharacter() + LoggerStringValues.RELEASED);
//        }
//    }
}
